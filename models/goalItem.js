class GoalItem {
  constructor(id, goal, objName, total, completed, step, color) {
    this.id = id;
    this.goal = goal;
    this.objName = objName;
    this.total = total;
    this.completed = completed;
    this.step = step;
    this.color = color;
  }
}

export default GoalItem;
